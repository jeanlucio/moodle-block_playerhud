{{!
    @template block_playerhud/sidebar_view
    Simplified view for the block sidebar with avatar and progress.
}}
<div class="block_playerhud_sidebar text-center">
    <div class="mb-2">
        <div class="mb-2">
            {{{userpicture}}}
        </div>
        <div class="fw-bold text-truncate" style="max-width: 100%;" title="{{username}}">
            {{username}}
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-1 small text-muted">
        <span class="badge bg-light text-dark border">{{#str}} level, block_playerhud {{/str}} {{level}}</span>
        <span>{{xp}} XP</span>
    </div>

    <div class="progress mb-3" style="height: 8px; border-radius: 4px; background-color: #e9ecef;">
        <div class="progress-bar bg-primary" role="progressbar" 
             style="width: {{progress}}%; border-radius: 4px;" 
             aria-valuenow="{{progress}}" aria-valuemin="0" aria-valuemax="100">
        </div>
    </div>

    <div class="d-grid gap-2">
        <a href="{{{viewurl}}}" class="btn btn-primary btn-sm shadow-sm">
            <i class="fa fa-briefcase" aria-hidden="true"></i> {{#str}} openbackpack, block_playerhud {{/str}}
        </a>
    </div>
</div>
